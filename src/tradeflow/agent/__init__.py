"""Agent Layer (§4.1).

Agents choose what to ask a tool, read back what it returns, and stop when the
input is insufficient. They never produce a number themselves — that boundary is
the whole point of the layer (§1.1).
"""
